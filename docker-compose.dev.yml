version: '3'
services:
  app:
    container_name: app-visium-api
    build:
      context: .
      target: dev
    ports:
      - "8000:8000"
    volumes:
      - .:/app

networks: 
  default:
    name: app-visium-network-api